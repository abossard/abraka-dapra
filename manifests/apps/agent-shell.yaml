version: 1
common:
  resourcesPaths: ../../components
apps:
  - appID: agent-shell
    appDirPath: ../..
    appPort: 8000
    appProtocol: http
    command: ["uv", "run", "uvicorn", "src.agent_shell.main:app", "--host", "0.0.0.0", "--port", "8000"]
